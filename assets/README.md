# Assets

Place the logo files here for Streamlit Cloud deployment.

Copy from `../../../Operations/`:
- `Logo Versuni.jfif`
- `Logo Roamler.jfif`

These files are loaded by `dashboard/branding.py`.
When running locally, the code also looks in `../Operations/` as a fallback.
